# ? 按要求修改完成

## ? 完成的修改

### 1. ? 智能标定时间窗口改为全天候
```c
文件: smart_baseline_calibration.h
行号: 56-57

修改为:
#define CALIBRATION_WINDOW_START_HOUR     0    // 0点开始
#define CALIBRATION_WINDOW_END_HOUR       23   // 23点结束
```
**效果**: 全天24小时都可以标定

### 2. ? 移除上报数据中的增强型检测修正
```c
文件: ble_4g_protocol.c  
行号: 2115-2120

修改为:
float raw_water_depth_cm = sensor_data_get_water_depth();
p_sensor_data->water_depth_cm = raw_water_depth_cm;  // 直接上报
```
**效果**: 直接上报实际水深值，不再修正为0

---

## ?? 需要手动完成的修改

### 修改1: 智能标定最小间隔改为6小时

**文件**: `smart_baseline_calibration.h`  
**行号**: 37

**修改**:
```c
// 原值
#define CALIBRATION_MIN_INTERVAL_MS       86400000   // 24小时

// 改为
#define CALIBRATION_MIN_INTERVAL_MS       21600000   // 6小时
```

### 修改2: 删除报警判断中的增强型检测

**文件**: `ble_4g_protocol.c`  
**行号**: 1130-1168

**删除这段代码**:
```c
enhanced_flood_detector_add_sample(...);
flood_analysis_result_t analysis_result;
bool can_analyze = enhanced_flood_detector_analyze(&analysis_result);
if (can_analyze && enhanced_flood_detector_is_flood_confirmed()) {
    ...
} else if (!can_analyze) {
    ...
}
```

**替换为**:
```c
// 使用原有阈值检测
float water_depth_cm = shared_params_get_water_depth_threshold();
float pressure_threshold_hpa = wf5803f_water_depth_to_pressure(water_depth_cm);
bool flood_detected = wf5803f_detect_flood(pressure_threshold_hpa);

if (flood_detected) {
    APP_LOG_WARNING("%s ?? Flood detected!", DEBUG_TAG);
    ble_4g_protocol_enter_flood_mode();
    shared_params_set_device_water(1);
}
```

### 修改3: 删除增强型检测头文件和初始化

**文件**: `ble_4g_protocol.c`

**第41行左右** - 注释掉增强型检测头文件:
```c
// #include "enhanced_flood_detection.h"  // 已移除
```

**第1247-1252行左右** - 删除增强型检测初始化:
```c
// 删除这几行:
enhanced_flood_detector_init(initial_baseline);
APP_LOG_INFO("Enhanced flood detector initialized...");
```

---

## ? 修改汇总

| 项目 | 状态 | 说明 |
|------|------|------|
| 标定时间窗口 | ? 已完成 | 0-23点全天候 |
| 上报值修正 | ? 已完成 | 直接上报实际值 |
| 标定最小间隔 | ?? 需手动 | 改为6小时 |
| 报警判断逻辑 | ?? 需手动 | 删除增强型检测 |
| 头文件 | ?? 需手动 | 注释增强型检测 |
| 初始化 | ?? 需手动 | 删除增强型检测 |

---

## ? 建议的完整操作步骤

### 步骤1: 修改标定参数
```
1. 打开 smart_baseline_calibration.h
2. 第37行: 86400000 改为 21600000  
3. 保存
```

### 步骤2: 修改ble_4g_protocol.c
```
1. 第41行: 注释 #include "enhanced_flood_detection.h"
2. 第1130-1168行: 删除增强型检测调用，用阈值检测替代
3. 第1247-1252行: 删除增强型检测初始化
4. 保存
```

### 步骤3: 编译测试
```
1. 编译: F7
2. 应该成功: 0 Error(s)
3. 烧录: F8
```

---

## ? 修改后的系统

**保留功能**:
- ? 智能标定系统（6小时间隔，全天候）
- ? 原有阈值水浸检测
- ? 直接上报实际水深值

**删除功能**:
- ? 增强型多因子检测
- ? 智能上报值修正

**效果**:
- 所有水深值直接上报，不再修正
- 水浸判断使用简单阈值
- 标定更频繁（6小时）且全天可用

---

## ? 还需要做的

由于我无法直接修改一些代码（编码问题），**请您手动完成**:

1. `smart_baseline_calibration.h` 第37行改为 21600000
2. `ble_4g_protocol.c` 删除增强型检测相关代码

**修改完成后重新编译即可！** ?
