# ?? 链接错误修复指南

## ? 错误原因

```
Error: L6218E: Undefined symbol smart_calibration_init
Error: L6218E: Undefined symbol smart_calibration_schedule
```

**原因**: `smart_baseline_calibration.c` 文件未添加到Keil项目中！

---

## ? 修复步骤

### 步骤1: 添加智能标定源文件 ?

**在Keil中操作**:

1. 在左侧 `Project` 窗口中找到 `user` 或 `Src\user` 组
2. 右键点击该组 → 选择 `Add Existing Files to Group 'user'...`
3. 浏览到文件位置:
   ```
   d:\gr5515\...\Src\user\smart_baseline_calibration.c
   ```
4. 选中该文件，点击 `Add`
5. 关闭对话框

---

### 步骤2: 移除增强型检测源文件

编译输出显示:
```
compiling enhanced_flood_detection.c...  ← 不应该编译这个！
```

**在Keil中操作**:

1. 在左侧 `Project` 窗口中找到 `enhanced_flood_detection.c`
2. 右键点击 → 选择 `Remove File 'enhanced_flood_detection.c'`
3. 确认移除（只是从项目中移除，不删除文件）

---

### 步骤3: 重新编译

已完成代码清理（删除未使用变量）

现在:

1. **Clean**: Project → Clean Targets
2. **Rebuild**: 按 F7
3. **预期**: 0 Error(s), 2-3 Warning(s)

---

## ? Keil项目中应该有的源文件

### 必须添加:
- ? `smart_baseline_calibration.c` - **必须添加！**

### 必须移除:
- ? `enhanced_flood_detection.c` - **必须移除！**

### 保留:
- ? `ble_4g_protocol.c`
- ? `wf5803f_driver.c`
- ? 其他所有现有文件

---

## ? 快速检查

### 在Keil左侧项目树中检查:

```
user 组（或 Src\user）
├── ble_4g_protocol.c        ?
├── smart_baseline_calibration.c  ← 应该有这个！
├── wf5803f_driver.c         ?
├── sensor_data_parser.c     ?
├── ...其他文件...
└── enhanced_flood_detection.c    ← 不应该有这个！
```

---

## ? 常见问题

### Q: 找不到 smart_baseline_calibration.c？

**A**: 文件应该在:
```
d:\gr5515\gr5515re\test1.0.1\test1.0.1\
  projects\ble\ble_multi_role\ble_app_uart_at\
    Src\user\smart_baseline_calibration.c
```

### Q: 添加后还是报错？

**A**: 
1. 确认添加的是 `.c` 文件（不是 `.h`）
2. Clean项目后重新编译
3. 检查文件路径是否正确

### Q: enhanced_flood_detection.c 找不到？

**A**: 如果项目树中没有，说明已经移除了，直接跳过步骤2

---

## ? 完成检查

添加/移除文件后，项目树应该:

- ? 有 `smart_baseline_calibration.c`
- ? 没有 `enhanced_flood_detection.c`

然后重新编译应该成功！

---

**立即操作：在Keil中添加/移除相应文件！** ?
