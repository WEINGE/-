# ? 环境稳定性检查阈值调整建议

## ? 您的观点完全正确！

**问题分析**:

```
标定的目的: 应对气压温度的逐渐变化
当前要求: 气压温度必须完全稳定
结果矛盾: 永远无法标定！
```

**您说得对**: 
- 如果气压温度不变化，就不需要标定
- 正是因为有缓慢变化，才需要定期标定
- 当前阈值太严格，会导致长期无法满足条件

---

## ? 解决方案

### 方案A: 放宽阈值（推荐）?

**文件**: `smart_baseline_calibration.h`  
**行号**: 45-46

**当前值**（太严格）:
```c
#define STABILITY_PRESSURE_VARIANCE_HPA   0.5f    // 0.5 hPa?
#define STABILITY_TEMP_VARIANCE_C         0.3f    // 0.3 °C?
```

**建议修改为**（放宽10倍）:
```c
#define STABILITY_PRESSURE_VARIANCE_HPA   5.0f    // 5.0 hPa?（放宽10倍）
#define STABILITY_TEMP_VARIANCE_C         3.0f    // 3.0 °C?（放宽10倍）
```

**效果**:
- ? 允许气压缓慢变化时也能标定
- ? 仍能防止剧烈波动时标定（如暴雨）
- ? 更容易满足标定条件

---

### 方案B: 完全禁用环境稳定性检查（最激进）

**如果想让标定更容易执行**，可以直接禁用这个检查：

**文件**: `smart_baseline_calibration.h`

**方法1**: 设置为极大值
```c
#define STABILITY_PRESSURE_VARIANCE_HPA   999.0f   // 实际禁用
#define STABILITY_TEMP_VARIANCE_C         999.0f   // 实际禁用
```

**方法2**: 使用禁用开关
```c
// 在代码中调用（如果有这个函数）
smart_calibration_set_stability_check_enable(false);
```

---

## ? 不同阈值的效果预测

### 当前值（0.5, 0.3）- 太严格 ?
```
允许的变化: 几乎不允许任何变化
满足概率: 很低（可能几天都不满足）
问题: 标定无法执行，失去意义
```

### 放宽10倍（5.0, 3.0）- 推荐 ?
```
允许的变化: 
  压力: 10分钟内变化 √5 ≈ 2.2 hPa以内
  温度: 10分钟内变化 √3 ≈ 1.7 °C以内

满足概率: 正常（大部分时候能满足）
效果: 
  ? 正常天气可标定
  ? 暴雨、剧烈波动时不标定
```

### 禁用检查（999.0, 999.0）- 最宽松 ??
```
允许的变化: 任何变化
满足概率: 永远满足
风险: 可能在暴雨等极端天气时标定
```

---

## ? 推荐配置

### 选择方案A（放宽10倍）

**理由**:
1. ? 允许正常的气压温度变化
2. ? 仍能防止极端天气时标定
3. ? 平衡了安全性和实用性

**修改方法**:
```
1. 打开 smart_baseline_calibration.h
2. 找到第45-46行
3. 修改为:
   #define STABILITY_PRESSURE_VARIANCE_HPA   5.0f
   #define STABILITY_TEMP_VARIANCE_C         3.0f
4. 保存
```

---

## ? 修改后的标定条件

### 修改前（5个条件，第3个太严格）
```
1. 时间间隔: 6小时-7天 ?
2. 时间窗口: 0-23点 ?
3. 环境稳定: 方差<0.5, <0.3 ? 太严格，很难满足
4. 无水浸: device_water=0 ?
5. 基准合理: 900-1100, 变化≤15 ?
```

### 修改后（更实用）
```
1. 时间间隔: 6小时-7天 ?
2. 时间窗口: 0-23点 ?
3. 环境稳定: 方差<5.0, <3.0 ? 更合理，容易满足
4. 无水浸: device_water=0 ?
5. 基准合理: 900-1100, 变化≤15 ?
```

---

## ? 实际场景对比

### 场景：气压缓慢上升

```
情况:
  10分钟内气压从1011升到1013（上升2 hPa）
  这是典型的需要标定的场景
  
当前阈值（0.5）:
  方差 ≈ 1.0 > 0.5 ?
  不能标定 ?
  问题: 正是因为气压在变化才需要标定！
  
放宽后（5.0）:
  方差 ≈ 1.0 < 5.0 ?
  可以标定 ?
  正确: 在缓慢变化时标定，更新基准
```

### 场景：暴雨（剧烈波动）

```
情况:
  10分钟内气压剧烈波动 995-1005-998-1002
  
当前阈值（0.5）:
  方差 ≈ 10.0 > 0.5 ?
  不能标定 ? 正确
  
放宽后（5.0）:
  方差 ≈ 10.0 > 5.0 ?
  不能标定 ? 仍然正确
  保护: 仍然防止极端天气时标定
```

---

## ? 建议操作

### 立即修改

**文件**: `smart_baseline_calibration.h`  
**行号**: 45-46

```c
// 修改这两行
#define STABILITY_PRESSURE_VARIANCE_HPA   5.0f    // 从0.5改为5.0
#define STABILITY_TEMP_VARIANCE_C         3.0f    // 从0.3改为3.0
```

**然后重新编译**: F7

---

## ? 总结

**您的分析**: ? 完全正确

**问题根源**: 设计时过度强调"稳定"，忽略了标定的真正目的

**解决方案**: 放宽阈值10倍

**预期效果**: 
- ? 正常天气可以标定
- ? 定期更新基准，消除误差
- ? 仍能防止极端天气时标定

---

**立即修改这两个参数，让智能标定真正发挥作用！** ?
