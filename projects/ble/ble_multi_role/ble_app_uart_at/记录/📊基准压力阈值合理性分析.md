# ? 基准压力阈值合理性分析

## ? 当前配置

```c
#define BASELINE_DRIFT_THRESHOLD_HPA   5.0f   // 基准漂移阈值
#define BASELINE_ABNORMAL_DELTA_HPA    15.0f  // 异常压力变化
```

---

## ? 基础知识

### 压力与水深关系
```
1 hPa ≈ 1 cm 水深
5 hPa ≈ 5 cm 水深
15 hPa ≈ 15 cm 水深
```

### 大气压力正常变化
```
日常变化:  1-3 hPa/天
天气系统:  5-10 hPa（高低压系统）
极端天气:  10-20 hPa（台风等）
正常范围:  960-1060 hPa
```

---

## ? 阈值1: 基准漂移 5.0 hPa

### 当前含义
```
当前baseline与实际的偏差 > 5 hPa
→ 需要标定
```

### 实际影响
```
如果气压上升了5 hPa:
  - 水深显示: +5 cm（误差）
  - 触发: 需要标定

如果气压下降了5 hPa:
  - 水深显示: -5 cm（可能显示负值或0）
  - 触发: 需要标定
```

### ?? 问题分析

#### 问题1: 阈值可能太大
```
5 cm误差才触发标定:
  - 如果要求精度 ±1 cm → 太大?
  - 如果只需粗略检测 → 可以?
```

#### 问题2: 这个阈值现在没有实际作用！
```
查看代码发现:
  标定触发条件主要是:
    1. 时间间隔（6小时）← 主要条件
    2. 环境稳定性 ← 主要条件
    3. 无水浸 ← 主要条件
  
  BASELINE_DRIFT_THRESHOLD 好像没有在判断中使用？
  可能只是用于其他地方的参考值
```

### 建议

#### 如果这个阈值有实际作用:

**高精度需求**（水浸检测阈值小于5cm）:
```c
#define BASELINE_DRIFT_THRESHOLD_HPA   2.0f  // 2cm误差就标定
```

**中等精度**（水浸检测阈值5-10cm）:
```c
#define BASELINE_DRIFT_THRESHOLD_HPA   3.0f  // 3cm误差标定
```

**当前配置**（粗略检测）:
```c
#define BASELINE_DRIFT_THRESHOLD_HPA   5.0f  // 5cm误差标定
```

---

## ? 阈值2: 异常压力变化 15.0 hPa

### 当前含义
```
压力变化 > 15 hPa
→ 认为可能有水浸
→ 不执行标定（保护机制）
```

### 实际影响
```
场景1: 真实水浸15cm
  - 压力增加: 15 hPa
  - 判断: 可能水浸
  - 操作: 不标定 ? 正确

场景2: 气压自然上升15 hPa（极少见）
  - 判断: 可能水浸
  - 操作: 不标定
  - 结果: 误判（但安全）
```

### ? 合理性评估

#### 优点
```
1. ? 作为水浸保护机制合理
   - 15cm水浸是明显的危险
   - 防止在有水时标定

2. ? 不会误触发
   - 正常气压变化很少超过15 hPa
   - 即使台风也较少超过20 hPa

3. ? 安全优先
   - 宁可不标定，也不能在有水时标定
```

#### 可能的调整

**更保守**（更小的水深就不标定）:
```c
#define BASELINE_ABNORMAL_DELTA_HPA    10.0f  // 10cm就认为异常
```

**当前配置**（平衡）:
```c
#define BASELINE_ABNORMAL_DELTA_HPA    15.0f  // 15cm认为异常 ? 推荐
```

**更宽松**（允许更大变化）:
```c
#define BASELINE_ABNORMAL_DELTA_HPA    20.0f  // 20cm认为异常
```

---

## ? 总体建议

### 阈值1: 基准漂移（5.0 hPa）

**当前值**: ?? 可能偏大（取决于精度要求）

**建议**:
```
如果水浸检测阈值 = 5cm:
  → 改为 2.0-3.0 hPa（确保误差不超过阈值）

如果水浸检测阈值 = 10cm:
  → 保持 5.0 hPa（可接受）

如果水浸检测阈值 > 15cm:
  → 保持 5.0 hPa（没问题）
```

### 阈值2: 异常压力变化（15.0 hPa）

**当前值**: ? 合理

**理由**:
```
1. 15cm水浸是明显危险
2. 正常气压很少变化这么大
3. 安全保护机制
4. 不建议修改
```

---

## ? 配置推荐

### 推荐配置1: 高精度（水浸阈值 ≤ 5cm）
```c
#define BASELINE_DRIFT_THRESHOLD_HPA   2.0f   // 2cm误差就标定
#define BASELINE_ABNORMAL_DELTA_HPA    10.0f  // 10cm认为异常（保守）
```

### 推荐配置2: 中等精度（水浸阈值 5-10cm）
```c
#define BASELINE_DRIFT_THRESHOLD_HPA   3.0f   // 3cm误差标定
#define BASELINE_ABNORMAL_DELTA_HPA    15.0f  // 15cm认为异常 ? 当前
```

### 推荐配置3: 低精度（水浸阈值 > 10cm）
```c
#define BASELINE_DRIFT_THRESHOLD_HPA   5.0f   // 5cm误差标定 ? 当前
#define BASELINE_ABNORMAL_DELTA_HPA    15.0f  // 15cm认为异常 ? 当前
```

---

## ? 实际检查

### 您的水浸检测阈值是多少？

**如果是5cm**:
```
baseline_drift = 5.0 hPa（合理）
abnormal_delta = 15.0 hPa（合理）
不需要修改 ?
```

**如果是3cm或更小**:
```
建议调整:
  baseline_drift = 2.0 hPa
  abnormal_delta = 10.0 hPa
```

---

## ? 结论

### BASELINE_DRIFT_THRESHOLD_HPA = 5.0f
```
合理性: ?? 取决于水浸检测阈值
建议: 
  - 如果检测阈值 ≥ 5cm → 保持5.0f ?
  - 如果检测阈值 < 5cm → 改为2.0-3.0f
```

### BASELINE_ABNORMAL_DELTA_HPA = 15.0f
```
合理性: ? 完全合理
建议: 保持15.0f（安全且实用）
```

---

**总体**: 异常压力变化15.0f很合理，基准漂移5.0f取决于您的精度要求。
**建议**: 先测试当前配置，根据实际效果调整。
