# ? 智能标定的数据采集机制

## ? 关键发现

**采样窗口**: 10次采样  
**配置位置**: `smart_baseline_calibration.h` 第47行
```c
#define STABILITY_CHECK_WINDOW  10  // 稳定性检查窗口：10次采样
```

---

## ?? 重要发现：智能标定系统的数据采集方式

### 当前实现方式

**查找结果**: 
- ? 有 `smart_calibration_add_sample()` 函数
- ? 但在 `ble_4g_protocol.c` 中**没有调用**这个函数

**这意味着**:
```
智能标定系统:
  ? 没有持续采集数据
  ? 没有维护10次采样的历史数据
  ? 环境稳定性检查可能无法正常工作
```

---

## ? 推测的工作方式

### 可能的实现（需要确认）

#### 方式1: 内部即时采样
```
每次调用 smart_calibration_schedule() 时:
  1. 立即连续读取10次传感器
  2. 计算这10次的方差
  3. 判断是否稳定
```

**问题**: 连续读取10次意义不大，都会很接近

#### 方式2: 使用历史数据（可能没有实现）
```
需要:
  1. 水位监测定时器每分钟调用 add_sample()
  2. 维护最近10次的历史数据
  3. 标定时检查这10次数据的方差
```

**问题**: 代码中没找到调用 `add_sample()` 的地方

---

## ? 应该如何工作？

### 理想的采集间隔

**基于水位监测定时器**:
```
水位监测: 每1分钟采集一次传感器数据
智能标定: 应该使用这些数据

理想流程:
  T+0分:  采样1 → add_sample()
  T+1分:  采样2 → add_sample()
  ...
  T+9分:  采样10 → add_sample()
  T+10分: 有10次历史数据

标定检查:
  使用最近10分钟的数据
  计算方差
  判断稳定性
```

**采集间隔应该是**: **1分钟**（与水位监测同步）

---

## ? 当前可能的问题

### 问题1: 数据未同步
```
水位监测定时器:
  - 每1分钟读取传感器
  - 但没有调用 smart_calibration_add_sample()
  
智能标定:
  - 可能没有历史数据
  - 环境稳定性检查可能失效
```

### 问题2: 检查窗口含义不明确
```
10次采样可能指:
  1. 最近10分钟的数据（理想）
  2. 立即连续读取10次（意义不大）
  3. 实际没有实现（检查总是通过/失败）
```

---

## ? 建议的修复

### 修复方案：在水位监测中添加采样

**位置**: `ble_4g_protocol.c` 的 `water_monitor_timer_handler()`

**添加**: 
```c
static void water_monitor_timer_handler(void *p_context)
{
    // ... 现有的传感器读取代码 ...
    
    if (sensor_data_read(&sensor_reading) && sensor_reading.data_valid) {
        // ... 现有的水浸检测代码 ...
        
        // ? 添加：将数据提供给智能标定系统
        smart_calibration_add_sample(
            sensor_reading.pressure_hpa,
            sensor_reading.temperature_c
        );
    }
}
```

**效果**:
- ? 每1分钟自动添加采样
- ? 维护最近10分钟的历史数据
- ? 环境稳定性检查能正常工作

---

## ? 修复后的时间线

```
T+0分:   水位监测 → 读传感器 → add_sample(1)
T+1分:   水位监测 → 读传感器 → add_sample(2)
T+2分:   水位监测 → 读传感器 → add_sample(3)
...
T+9分:   水位监测 → 读传感器 → add_sample(10)

T+6小时: 标定检查
         → 获取最近10次数据（最近10分钟）
         → 计算方差
         → 判断是否满足稳定性条件
```

**采集间隔**: **1分钟**  
**检查窗口**: **最近10分钟**

---

## ?? 需要确认

### 检查智能标定内部实现

**查看** `smart_baseline_calibration.c` 中的稳定性检查：
1. 是否有维护历史数据？
2. 是否使用 `add_sample()` 的数据？
3. 还是即时采样？

### 如果没有实现历史数据维护

**两个选择**:
1. **添加数据同步**（推荐）- 让水位监测调用 `add_sample()`
2. **禁用稳定性检查** - 设置方差阈值为极大值

---

## ? 回答您的问题

**采集间隔**:
- **理想情况**: 1分钟（与水位监测同步）
- **可能现状**: 可能没有持续采集，需要确认实现

**建议**:
1. 查看智能标定系统的实际实现
2. 如果没有历史数据维护，添加数据同步
3. 或者直接禁用稳定性检查（设置阈值为极大值）

---

**采集间隔应该是1分钟，但需要确认代码中是否实现了数据同步！** ?
