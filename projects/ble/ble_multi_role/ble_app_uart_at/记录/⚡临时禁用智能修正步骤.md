# ?? 紧急：临时禁用智能修正

## ? 问题确认

**14cm快速水浸仍上报0 = 增强型检测严重误判！**

---

## ? 立即修复（手动操作）

### 修改文件：`ble_4g_protocol.c`

**找到第2115-2143行**，修改为：

```c
// 修复：使用统一的水深计算函数
float raw_water_depth_cm = sensor_data_get_water_depth();

// ?? 临时禁用智能修正，直接上报实际值
p_sensor_data->water_depth_cm = raw_water_depth_cm;
APP_LOG_INFO("%s Water depth (DIRECT): %.1f cm", DEBUG_TAG, raw_water_depth_cm);
```

**删除或注释掉**第2118-2143行之间的所有智能修正逻辑！

---

## ? 详细步骤

### 步骤1: 打开文件
```
文件: d:\gr5515\...\ble_4g_protocol.c
行数: 2115-2143
```

### 步骤2: 找到这段代码

应该能看到：
```c
// ? 增强型检测修正：只有在增强型可用且判断"非水浸"时才修正为0
flood_analysis_result_t temp_analysis;
bool enhanced_available = enhanced_flood_detector_analyze(&temp_analysis);

if (enhanced_available && !enhanced_flood_detector_is_flood_confirmed()) {
    ...
```

### 步骤3: 全部注释掉

在每行前面加 `//`，或者用 `/*  */` 包围

### 步骤4: 添加简单逻辑

```c
float raw_water_depth_cm = sensor_data_get_water_depth();
p_sensor_data->water_depth_cm = raw_water_depth_cm;
```

---

## ? 修改后的完整代码

```c
// 第2115行开始
// ? 修复：使用统一的水深计算函数
float raw_water_depth_cm = sensor_data_get_water_depth();

// 直接上报实际值（禁用智能修正）
p_sensor_data->water_depth_cm = raw_water_depth_cm;
APP_LOG_INFO("%s Water depth: %.1f cm", DEBUG_TAG, raw_water_depth_cm);

// 更新传感器状态为正常
shared_params_set_sensor_status(0);
result = true;
```

---

## ? 为什么这样做？

```
当前问题:
  增强型检测误判 → 把真实水浸判断为"非水浸"
  → 触发智能修正 → 上报0
  → 平台看不到危险！

临时方案:
  禁用智能修正
  → 直接上报实际值
  → 平台能看到真实情况
  
后续:
  1. 先让系统能正常工作
  2. 再慢慢调整增强型检测参数
  3. 调好后再启用智能修正
```

---

## ? 操作

1. **修改代码**（如上）
2. **编译**: F7
3. **烧录**: F8  
4. **测试**: 应该能正常上报了

---

**这个修改会让系统回到"直接上报实际值"的状态，确保水浸能被检测到！**

**增强型检测和智能标定仍在后台运行（用于本地报警判断），只是不修正上报值了。**

---

**请立即修改并测试！** ?
