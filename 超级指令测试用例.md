# 📋 超级指令功能测试用例

## 🎯 测试目标

验证超级指令和AT指令模式管理的完整功能，确保：
1. **指令模式管理**正确工作
2. **超级指令**可以直接使用
3. **普通AT指令**需要指令模式
4. **数据获取**功能正常

---

## 🧪 测试环境准备

### **硬件要求**
- GR5515开发板
- 串口调试工具 (如串口助手、Tera Term等)
- USB转串口线

### **软件配置**
- 波特率: 115200
- 数据位: 8
- 停止位: 1
- 校验位: 无
- 流控制: 无

---

## 📝 测试用例

### **测试用例1: 指令模式管理**

#### **1.1 进入指令模式**
```bash
发送: +++
期望返回: OK
```

#### **1.2 在指令模式下使用AT指令**
```bash
发送: AT:TEST
期望返回: TEST_OK

发送: AT:VERSION?
期望返回: +VERSION:<版本信息>

发送: AT:ADDR?
期望返回: +ADDR:<MAC地址>
```

#### **1.3 退出指令模式**
```bash
发送: AT:ENTM
期望返回: OK
```

#### **1.4 指令模式外无法使用普通AT指令**
```bash
发送: AT:TEST
期望返回: (无响应)

发送: AT:VERSION?  
期望返回: (无响应)
```

---

### **测试用例2: 超级指令功能**

#### **2.1 设备信息查询**
```bash
发送: adminAT+IMEI?
期望返回: +IMEI:860123456789012

发送: adminAT+ICCID?
期望返回: +ICCID:89860123456789012345

发送: adminAT+BUILD?
期望返回: +BUILD:Sep 24 2024 10:30:25
```

#### **2.2 网络信息查询**
```bash
发送: adminAT+CSQ?
期望返回: +CSQ:25,0

发送: adminAT+CREG?
期望返回: +CREG:0,1,1234,12345678

发送: adminAT+GPS?
期望返回: +GPS:1,39.904200,116.407400,50.0,0.0,0.0
```

#### **2.3 系统信息查询**
```bash
发送: adminAT+CCLK?
期望返回: +CCLK:"24/09/24,10:30:25+32"

发送: adminAT+RUNST?
期望返回: +RUNST:3600
```

#### **2.4 传感器信息查询**
```bash
发送: adminAT+SENSOR?
期望返回: +SENSOR:+1008.37 +12.5 0000000 00 3A
或: +SENSOR:+0000.00 +00.0 0000000 56 00 (数据无效时)

发送: adminAT+STATUS?
期望返回: +STATUS:DEV=0,SENSOR=0,COMM=1
```

---

### **测试用例3: 混合使用场景**

#### **3.1 超级指令与指令模式混合使用**
```bash
# 1. 使用超级指令 (无需指令模式)
发送: adminAT+CSQ?
期望返回: +CSQ:25,0

# 2. 进入指令模式
发送: +++
期望返回: OK

# 3. 使用普通AT指令
发送: AT:CSQ?
期望返回: +CSQ:25,0

# 4. 继续使用超级指令 (在指令模式下也可用)
发送: adminAT+GPS?
期望返回: +GPS:1,39.904200,116.407400,50.0,0.0,0.0

# 5. 退出指令模式
发送: AT:ENTM
期望返回: OK

# 6. 超级指令仍然可用
发送: adminAT+STATUS?
期望返回: +STATUS:DEV=0,SENSOR=0,COMM=1
```

---

### **测试用例4: 错误处理**

#### **4.1 无效超级指令**
```bash
发送: adminAT+INVALID?
期望返回: ERROR:Unsupported admin command
```

#### **4.2 指令模式下的无效AT指令**
```bash
发送: +++
期望返回: OK

发送: AT:INVALID
期望返回: ERROR (或其他错误信息)
```

#### **4.3 格式错误的指令**
```bash
发送: adminAT+
期望返回: ERROR:Unsupported admin command

发送: +++invalid
期望返回: (无响应，因为不是标准的+++格式)
```

---

## 🔍 验证要点

### **功能验证**
- ✅ `+++` 能正确进入指令模式并返回 `OK`
- ✅ `AT:ENTM` 能正确退出指令模式并返回 `OK`
- ✅ 普通AT指令只在指令模式下响应
- ✅ 超级指令在任何状态下都能响应
- ✅ 所有查询指令返回预期格式的数据

### **数据格式验证**
- ✅ **IMEI格式**: 15位数字
- ✅ **ICCID格式**: 20位数字
- ✅ **CSQ格式**: `+CSQ:<rssi>,<ber>`
- ✅ **GPS格式**: `+GPS:<status>,<lat>,<lon>,<alt>,<speed>,<course>`
- ✅ **传感器格式**: `+SENSOR:+XXXX.XX +XX.X XXXXXXX XX XX`
- ✅ **状态格式**: `+STATUS:DEV=X,SENSOR=X,COMM=X`

### **性能验证**
- ✅ 指令响应时间 < 500ms
- ✅ 连续指令处理正常
- ✅ 长时间运行稳定

---

## 🐛 常见问题排查

### **问题1: 发送+++无响应**
**可能原因**:
- 串口连接问题
- 波特率设置错误
- 设备未正确初始化

**解决方案**:
- 检查串口连接和配置
- 重启设备后重试
- 确认AT指令处理器已初始化

### **问题2: 超级指令无响应**
**可能原因**:
- 指令格式错误
- 函数未正确注册
- 编译问题

**解决方案**:
- 检查指令格式是否为 `adminAT+<cmd>?`
- 验证AT指令表中的注册
- 重新编译并下载程序

### **问题3: AT指令在指令模式外有响应**
**可能原因**:
- 指令模式检查逻辑有误
- 预处理函数未正确调用

**解决方案**:
- 检查 `uart_at_preprocess_command` 函数
- 验证指令模式状态管理
- 检查AT指令解析流程

---

## 📊 测试记录模板

### **测试环境**
- **设备型号**: GR5515开发板
- **固件版本**: [填写版本号]
- **测试日期**: [填写日期]
- **测试人员**: [填写姓名]

### **测试结果记录**
| 测试用例 | 发送指令 | 期望结果 | 实际结果 | 状态 | 备注 |
|----------|----------|----------|----------|------|------|
| 1.1 | `+++` | `OK` | | ✅/❌ | |
| 1.2 | `AT:TEST` | `TEST_OK` | | ✅/❌ | |
| 1.3 | `AT:ENTM` | `OK` | | ✅/❌ | |
| 2.1 | `adminAT+IMEI?` | `+IMEI:...` | | ✅/❌ | |
| 2.2 | `adminAT+CSQ?` | `+CSQ:...` | | ✅/❌ | |
| ... | ... | ... | ... | ... | ... |

### **问题记录**
| 问题描述 | 复现步骤 | 预期行为 | 实际行为 | 解决方案 | 状态 |
|----------|----------|----------|----------|----------|------|
| | | | | | |

---

## 🎉 测试总结

通过以上测试用例，可以全面验证：

1. **✅ 指令模式管理**: 正确实现了`+++`进入和`AT:ENTM`退出
2. **✅ 超级指令功能**: 所有`adminAT+`指令都能正常工作
3. **✅ 数据获取完整性**: 支持设备信息、网络状态、传感器数据等
4. **✅ 错误处理机制**: 对无效指令有正确的错误响应
5. **✅ 兼容性保证**: 不影响现有的AT指令功能

这套测试用例确保了超级指令功能的**稳定性**、**完整性**和**易用性**！🚀 